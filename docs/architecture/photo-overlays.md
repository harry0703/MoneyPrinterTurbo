# Фото-вставки из локальной папки

Наложение фотографий пользователя на сильные фразы сценария. Фича выключена по
умолчанию (`photo_enabled = false`), поведение пайплайна без неё не меняется.

Дата разработки: 2026-08-13.

---

## Зачем

Тот же приём, что и GIF-вставки (`gif-overlays.md`), но с собственным контентом:
скриншоты, продукты, кадры — то, чего нет в стоковых каталогах. Источник —
локальная папка, поэтому ни внешнего API, ни ключей не нужно.

Отличия от гифок, зафиксированные осознанно:

- **Показ короткой вспышкой** (`photo_duration`, деф. 2.0 сек ± 0.5), а не всё окно
  фразы: статичное фото дольше не живёт, начинает читаться как зависший кадр.
  Джиттер детерминированно выводится из имени файла.
- **Анимация появления обязательна** — статичной картинке нужен вход, иначе она
  выглядит как склейка. Пул из трёх дешёвых анимаций, без per-frame поворотов.

---

## Поток данных

```
сценарий → озвучка → субтитры (или gif-timeline.srt из TTS-таймингов)
   ↓
generate_photo_overlays()             окна ((start, end), текст)
   ↓
photo_library.discover_photo_themes() папка → темы (файлы + описания)
   ↓
llm.generate_photo_theme_plan()       один вызов LLM: [{line, theme}]
   │   фолбэк: generate_photo_moments() → индексы сильных строк
   │   фолбэк фолбэка: _spread_photo_moments() — равномерно по ролику
   ↓
ThemedPhotoPicker: файл из нужной темы → [{path, start, end}]
   ↓
video.generate_video(photo_overlays=[...])
   ↓
_build_photo_overlay_clips() → CompositeVideoClip поверх гифок и субтитров
```

Тайминги переиспользуют гифочный код (`_gif_timeline` / `_subtitle_windows`):
окна из SRT, без субтитров — временный SRT из TTS-таймингов, без таймингов —
вставки не добавляются.

---

## Файлы

| Файл | Что делает |
|---|---|
| `app/services/photo_library.py` | `discover_photo_themes()`, `PhotoTheme`, `ThemedPhotoPicker`, `list_photo_files()` |
| `app/services/llm.py` | `generate_photo_theme_plan()` / `_normalize_photo_theme_plan()` — темы по строкам; `generate_photo_moments()` / `_normalize_photo_moments()` — фолбэк |
| `app/services/task.py` | `generate_photo_overlays()`, `_plan_photo_placements()`, `_validate_photo_overlay_params()`, `_spread_photo_moments()`, `_select_photos()` |
| `app/services/video.py` | `_build_photo_overlay_clips()`, `_photo_card_image()`, `_pick_photo_animation()`, `_ease_out()`; общие с гифками `_overlay_box()` и `_shadow_image()` |
| `app/models/schema.py` | Поля `photo_enabled`, `photo_dir`, `photo_amount`, `photo_size`, `photo_duration`, `photo_animation` |
| `webui/Main.py` | `_render_photo_overlay_settings()` — панель в «Настройках видео» |
| `webui/i18n/*.json` | 11 строк, en/ru/zh + английский фолбэк в остальных |
| `cli.py` | флаги `--photo-*` + валидаторы значений и существования папки |
| `test/services/test_photo_overlays.py` | тесты разметки, таймингов, карточек |
| `test/services/test_photo_library.py` | тесты тем: раскладка папки, манифест, выдача файлов |
| `test/services/test_cli.py` | 4 теста маппинга и валидации `--photo-*` |

---

## Темы вместо алфавита

Материал скаутится темами (`docs/playbook/media-scouting.md`): несколько запросов,
пачка файлов на каждый. Матчить LLM'ом каждый файл смысла нет — из имени модель
всё равно видит только тему, а промпт распухает. Поэтому единица сопоставления —
**тема**, конкретный файл внутри неё выбирает код.

`photo_library.discover_photo_themes()` определяет темы так:

| Раскладка папки | Тема |
|---|---|
| `photos/spiderman/*.jpg` | `spiderman` (имя подпапки) |
| `photos/20-spider-01.jpg` | `spider` (первый нечисловой токен имени) |
| `photos/01.jpg` | `misc` |

Числовые префиксы задают порядок показа внутри темы и в имя темы не входят.
Описания тем — опциональный `themes.json` в корне `photo_dir`
(`{"spider": "конфликт с прокатом ..."}`); без него в промпт уходят 3 примера имён
файлов. Битый json игнорируется.

## Как выбираются моменты

`llm.generate_photo_theme_plan()` получает пронумерованные строки сценария и список
тем, возвращает `[{"line": 3, "theme": "spider"}, ...]`. Нормализация выкидывает
неизвестные темы, повторы строк и выходы за диапазон. Соседние строки об одном могут
делить тему — это норма, а не дубль.

Файл внутри темы выдаёт `ThemedPhotoPicker`: по порядку имён, курсор на тему.
Когда пул темы кончился — курсор идёт по кругу: повторить фото нужной темы лучше,
чем показать чужую (за 60 сек это заметно на теме из 2 файлов, ловится числом
файлов, а не кодом).

Фолбэки, оба сохраняют прежнее поведение:

1. тем меньше двух или план пустой → `llm.generate_photo_moments()` — индексы
   сильных строк, фото раздаются по сортировке имён, `_select_photos()` берёт их
   равномерно по всему списку;
2. и это пусто → `_spread_photo_moments()`, равномерно по ролику.

`amount = min(photo_amount, файлов, окон)`.

---

## Показ и анимации

Начало — **строго старт своей фразы**; длительность `photo_duration ± 0.5` сек
(джиттер детерминирован именем файла), обрезается началом следующей вставки и концом
таймлайна. Окна субтитров бывают короче показа фото, и раньше старт сдвигался за конец
предыдущей карточки — на плотной раскладке набегал дрейф до +9 сек, и подобранное по
смыслу фото приезжало через полтакта после своей фразы. Теперь пересечение решается
обрезкой: карточка короче — не беда, карточка не на своей строке — беда. Если после
обрезки остаётся меньше `_PHOTO_MIN_DISPLAY` (1.0 сек), вставка выбрасывается: такая
вспышка всё равно не читается. Замер на 57-сек ролике: 20 запрошенных вставок → 17
показанных, дрейф 0. Раскладка карточки — целиком гифочная (`_gif_overlay_placement`):
чередование сторон, наклон к центру, разброс по высоте, скругление, тень.

Анимации (`photo_animation`, деф. `random` — детерминированный выбор из пула по
имени файла):

| Анимация | Что делает | Механика |
|---|---|---|
| `pop` | scale 0.85→1.0 за 0.35 с, ease-out | `resized(λt)` + позиция с фиксированным центром |
| `slide` | въезд со стороны размещения | `with_position(λt)` от -w / video_width |
| `kenburns` | зум фото 1.0→1.10 внутри карточки всё время показа | inner `resized(λt)` в `CompositeVideoClip` фиксированного размера |

Плюс кросс-фейд 0.25 с на входе и выходе у всех.

Ключевое отличие от гифок по стоимости: для `pop`/`slide` карточка (фото со
скруглением + тень + наклон) **запекается один раз в PIL** в единую RGBA-картинку —
`_photo_card_image()`; per-frame `vfx.Rotate` не используется. PIL `rotate()`
крутит против часовой на положительном угле, как и `vfx.Rotate`, поэтому знак
угла общий. `kenburns` внутри окна анимирует контент, поэтому идёт гифочным путём
(маска + `vfx.Rotate` на карточку и тень); inner-зум стартует с 1.0 и только
растёт — рамка всегда покрыта, подмена маски безопасна.

Анимация `tilt` (доворот на место) отложена сознательно: это per-frame поворот с
переменным углом, дороже всего пула вместе взятого.

---

## Конфигурация

`VideoParams` (ключей в `config.toml` нет — источник локальный). Из CLI —
`--photo-enabled/--no-photo-enabled`, `--photo-dir`, `--photo-amount`,
`--photo-size`, `--photo-duration`, `--photo-animation`; несуществующий `--photo-dir`
отбрасывается ещё argparse'ом, до создания задачи.

| Поле | Деф. | Что делает |
|---|---|---|
| `photo_enabled` | `false` | вкл/выкл фичу целиком |
| `photo_dir` | `""` | папка с jpg/jpeg/png/webp |
| `photo_amount` | `5` | максимум вставок (1–40) |
| `photo_size` | `0.63` | доля ширины кадра (0.1–0.9); высота режется потолком 0.62 кадра |
| `photo_duration` | `2.0` | средняя длительность показа, сек (0.8–6.0) |
| `photo_animation` | `"random"` | `random / pop / slide / kenburns` |

---

## Поведение при сбоях

Единственный жёсткий отказ — в preflight: включённая фича с пустым/несуществующим
`photo_dir` или папкой без картинок валит задачу **до** трат на LLM и TTS
(`_validate_photo_overlay_params`, только для `stop_at in (materials, video)`).
Дальше — тихая деградация, ролик собирается без фото:

| Ситуация | Что происходит |
|---|---|
| Папка опустела между preflight и рендером | лог + пустой список |
| Нет таймингов (нет SRT и sub_maker) | вставки не добавляются |
| LLM вернул мусор | нормализация чистит, до 5 ретраев, потом равномерный фолбэк |
| Битый файл картинки | ловится в цикле `_build_photo_overlay_clips()`, остальные рендерятся |
| EXIF-поворот телефонных фото | `ImageOps.exif_transpose()` перед рендером |

---

## Проверено

- `test/services/test_photo_overlays.py` + `test_photo_library.py` (разметка тем,
  preflight, фолбэки, тайминги без дрейфа, раскладка папки, манифест, выбор анимации,
  геометрия карточки, построение клипов с рендером кадров).
- Весь сьют: **730 passed, 11 skipped**; ruff чистый.
- Реальный рендер 1080×1920 со всеми тремя анимациями — покадрово проверены
  pop (масштаб растёт), slide (карточка едет со своей стороны), kenburns
  (рамка и тень неподвижны, контент зумится).
- Живой прогон 14.08 (57-сек озвучка Gemini, набор `mix20` из 4 тем, live LLM):
  план 20 пар «строка → тема», показано 15 вставок, покадрово сверено —
  «Колобка озвучил Харламов» → портрет Харламова, «Режиссёр сцепился в сети» →
  фото Маслова, «в день „Паука“» → кадр Человека-паука, «стартовый уикенд принёс» →
  новость о кассе. Карточки ×2.25 по площади.

**Не проверено:** панель в браузере (Streamlit не запускался).

---

## Ограничения и открытые вопросы

1. **Гифки + фото одновременно**: моменты размечаются двумя независимыми вызовами
   LLM, вставки могут совпасть по времени и месту. Пока считаем, что включают
   что-то одно; при реальном спросе — общая разметка одним вызовом.
2. **В legacy-пути смысл берётся из имени темы**, не из содержимого файла: тема
   `spider` под строкой про «Человека-паука» — это совпадение слов, а не понимание
   кадра. При включённой накопительной библиотеке это ограничение снято: vision
   размечает содержимое, а pgvector сопоставляет visual brief с caption. Настройка
   и отличия нового пути описаны в `photo-asset-library.md`.
